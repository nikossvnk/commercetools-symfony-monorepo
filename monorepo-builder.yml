parameters:
    package_directories:
        - 'src'
    # for "merge" command
    data_to_append:
        require-dev:
            symplify/monorepo-builder: '^5.2'

        autoload:
            psr-4:
                Commercetools\Symfony\: 'src/'

    # for "split" command
    directories_to_repositories:
        src/CartBundle: 'git@github.com:nikossvnk/commercetools-symfony-CartBundle.git'
        src/CtpBundle: 'git@github.com:nikossvnk/commercetools-symfony-CtpBundle.git'
